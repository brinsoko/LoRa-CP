# HTTP-only Caddyfile for IP testing (no TLS). Replace with a domain + tls when ready.
{
	auto_https off
}

:80 {
	encode zstd gzip

	@static {
		path /static/* /favicon.ico
	}

	handle @static {
		file_server
	}

	handle {
		reverse_proxy web:5000
	}
}
